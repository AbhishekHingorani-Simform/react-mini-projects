import OptionsManager from './OptionsManager';

/**
 * DecisionWheel — entry point for the Decision Wheel mini-project.
 *
 * Currently renders the OptionsManager that lets users build the list
 * of choices. The spinning wheel visualisation is a planned future
 * enhancement that will consume the options list exposed here.
 */
const DecisionWheel = () => {
  return <OptionsManager />;
};

export default DecisionWheel;
