/**
 * OptionsInput — backward-compatible re-export of OptionsManager.
 *
 * The canonical component is OptionsManager (OptionsManager.jsx).
 * This file preserves the original public name so that any existing
 * imports continue to work without modification.
 */
export { default } from './OptionsManager';
